{
  "id": "no-sibling-abort",
  "description": "Remove sibling tool call abort — let each tool succeed or fail independently",
  "target": "both",
  "tested_versions": ["2.1.63"],
  "steps": [
    {
      "type": "find_replace",
      "id": "remove_sibling_abort",
      "find": "if(this.hasErrored&&!this.allToolsAreWriteOrEdit())return\"sibling_error\"",
      "replace": "",
      "mode": "literal",
      "pad_to_length": true
    }
  ],
  "assertions": {
    "pre": [{ "present": "if(this.hasErrored&&!this.allToolsAreWriteOrEdit())return\"sibling_error\"" }]
  }
}
