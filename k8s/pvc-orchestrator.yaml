apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-orchestrator-data
  namespace: claude-runner
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
