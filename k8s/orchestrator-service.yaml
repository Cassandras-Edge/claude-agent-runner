apiVersion: v1
kind: Service
metadata:
  name: claude-orchestrator
  namespace: claude-runner
spec:
  type: ClusterIP
  selector:
    app: claude-orchestrator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: ws
      port: 8081
      targetPort: 8081
